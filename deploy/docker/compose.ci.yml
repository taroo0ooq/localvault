# CI / DAST — no tunnel tokens required
services:
  vault-api:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile.vault-api
    ports:
      - "8443:8443"
    environment:
      VAULT_LISTEN: "0.0.0.0:8443"
      VAULT_DB: "/tmp/vault-ci.db"
      VAULT_TUNNEL_MODE: "0"
